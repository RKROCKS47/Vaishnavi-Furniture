diff --git a/README.md b/README.md
index 02c999e..d8a90aa 100644
--- a/README.md
+++ b/README.md
@@ -1,56 +1,32 @@
 # Vaishnavi Furniture 🪑
 
-Vaishnavi Furniture is a modern furniture and cooling solutions website built to showcase products like sofas, beds,mandir, wardrobes, and cnc designs for a local retail business. It focuses on clean UI, simple navigation, and a responsive layout suitable for real-world use.
+Website for Vaishnavi Furniture, a furniture retail shop in Risod, Maharashtra — showcasing CNC designs, mandirs, beds, wardrobes, kitchens and more, with cart, checkout, and WhatsApp ordering.
 
 ## 🚀 Live Demo
 
-- Live Site: https://vaishnavi-furniture-4b2d9.web.app/ 
+- https://vaishnavifurniture.in/
 
 ## ✨ Features
 
-- Responsive layout for mobile, tablet, and desktop users.  
-- Product‑centric sections for furniture and coolers.  
-- Simple, clean navigation for quick access to key pages.  
-- Business‑ready design suitable for local shops and showrooms.  
-- Easy to extend with backend / database for dynamic products.
+- Product gallery loaded from Firestore, with category filters
+- Cart & checkout flow, orders forwarded via WhatsApp
+- Customer reviews section
+- Admin panel (Firebase Auth): add/edit/delete products, image uploads via Cloudinary, order management
+- Responsive Bootstrap layout for mobile, tablet, and desktop
 
 ## 🛠 Tech Stack
 
-- Frontend: HTML, CSS, JavaScript  
-- Hosting: Firebase Hosting (web.app)  
-- Tools: Git, GitHub
+- HTML, CSS, JavaScript + Bootstrap 5
+- Firebase Hosting, Firestore, Firebase Auth
+- Cloudinary (image uploads)
 
+## Deploy
 
-# 1. Clone the repository
-git clone https://github.com/<your-username>/<your-repo-name>.git
-
-# 2. Move into the project folder
-cd <your-repo-name>
-
-# 3. Install dependencies (if applicable)
-npm install
-
-📦 Build & Deploy
-# 4. Start the development server
-npm run dev     # or npm start / npm run serve depending on your setup
-
-# Build for production (if using a framework)
-npm run build
-
-# Firebase deployment (example)
-firebase login
-firebase init hosting
+```bash
 firebase deploy
+```
 
-📬 Contact
-For inquiries about the website or business:
-
-Owner: Vaishnavi Furniture
-
-Location: Risod, Maharashtra, India 
-
-Email:Rohitingale586@gmail.com
-
-Phone:7972803288
-
+## 📬 Contact
 
+Vaishnavi Furniture, Risod, Maharashtra, India
+Phone: +91 79728 03288
diff --git a/admin/admin-orders.html b/admin/admin-orders.html
index ffbbcb7..0aa11a2 100644
--- a/admin/admin-orders.html
+++ b/admin/admin-orders.html
@@ -1,17 +1,45 @@
 <!DOCTYPE html>
 <html lang="en">
 <head>
+<script type="module">
+  import { auth } from "../assets/js/firebase.js";
+  import { onAuthStateChanged } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js";
+  onAuthStateChanged(auth, (user) => {
+    if (!user) window.location.href = "login.html";
+  });
+</script>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Admin Orders | Vaishnavi Furniture</title>
+  <meta name="robots" content="noindex, nofollow" />
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
+  <style>
+    .filter-pill { cursor: pointer; }
+    .filter-pill.active { outline: 2px solid #0d6efd; outline-offset: 1px; }
+    details summary { cursor: pointer; }
+  </style>
 </head>
 <body class="bg-light">
   <div class="container py-4">
-    <h2 class="mb-4">🧾 Customer Orders</h2>
-    <ul id="order-list" class="list-group"></ul>
+    <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
+      <h2 class="mb-0">🧾 Customer Orders</h2>
+      <div class="d-flex gap-2">
+        <a href="admin.html" class="btn btn-outline-secondary btn-sm">← Products</a>
+        <button id="logout-btn" class="btn btn-outline-danger btn-sm">Logout</button>
+      </div>
+    </div>
+
+    <div class="row g-2 mb-3">
+      <div class="col-md-6">
+        <input id="search" type="search" class="form-control" placeholder="🔍 Search by name or phone…" />
+      </div>
+    </div>
+    <div id="filter-pills" class="d-flex flex-wrap gap-2 mb-3"></div>
+    <div id="order-list" class="d-flex flex-column gap-3"></div>
   </div>
 
+  <div id="toast-container" class="toast-container position-fixed bottom-0 end-0 p-3" style="z-index:1080"></div>
+
   <!-- Edit Product Modal -->
   <div class="modal fade" id="editProductModal" tabindex="-1">
     <div class="modal-dialog">
@@ -51,13 +79,15 @@
   </div>
 
   <script type="module">
-    import { db } from "../assets/js/firebase.js";
+    import { db, auth } from "../assets/js/firebase.js";
     import {
       collection,
       getDocs,
       updateDoc,
-      doc
+      doc,
+      arrayUnion
     } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js";
+    import { signOut } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js";
     import {
       getStorage,
       ref,
@@ -65,67 +95,201 @@
       getDownloadURL
     } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-storage.js";
 
+    // ── Order lifecycle ──────────────────────────────────────────
+    const STATUSES = [
+      { id: "received",         label: "Received",          badge: "secondary" },
+      { id: "confirmed",        label: "Confirmed",         badge: "info" },
+      { id: "in_production",    label: "In Production",     badge: "warning" },
+      { id: "ready",            label: "Ready for Delivery", badge: "primary" },
+      { id: "out_for_delivery", label: "Out for Delivery",  badge: "dark" },
+      { id: "delivered",        label: "Delivered",         badge: "success" },
+      { id: "cancelled",        label: "Cancelled",         badge: "danger" },
+    ];
+    // Map old status values to the new pipeline
+    const LEGACY_STATUS = {
+      new: "received",
+      "in-progress": "in_production",
+      shipped: "out_for_delivery",
+      delivered: "delivered",
+    };
+
     const orderCollection = collection(db, "orders");
     const productCollection = collection(db, "products");
     const storage = getStorage();
 
+    let allOrders = [];
+    let activeFilter = "all";
+    let searchTerm = "";
+
+    const esc = (s) => String(s ?? "").replace(/[&<>"']/g, (c) => ({
+      "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;"
+    }[c]));
+    const normStatus = (s) => LEGACY_STATUS[s] || s || "received";
+    const statusMeta = (id) => STATUSES.find((x) => x.id === id) || STATUSES[0];
+    const fmtDate = (iso) => {
+      if (!iso) return "—";
+      return new Date(iso).toLocaleString("en-IN", {
+        day: "numeric", month: "short", hour: "2-digit", minute: "2-digit",
+      });
+    };
+
+    function showToast(msg, type = "success") {
+      const container = document.getElementById("toast-container");
+      const el = document.createElement("div");
+      el.className = `toast align-items-center text-bg-${type} border-0`;
+      el.setAttribute("role", "alert");
+      el.innerHTML = `<div class="d-flex"><div class="toast-body"></div>
+        <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button></div>`;
+      el.querySelector(".toast-body").textContent = msg;
+      container.appendChild(el);
+      new bootstrap.Toast(el, { delay: 2500 }).show();
+    }
+
+    let productMap = {};
+
     async function loadOrders() {
       const productSnap = await getDocs(productCollection);
-      const products = {};
-      productSnap.forEach(p => products[p.id] = p.data());
+      productMap = {};
+      productSnap.forEach((p) => (productMap[p.id] = p.data()));
 
       const orderSnap = await getDocs(orderCollection);
-      const list = document.getElementById("order-list");
-      list.innerHTML = "";
+      allOrders = [];
+      orderSnap.forEach((d) => allOrders.push({ id: d.id, ...d.data() }));
+      // Newest first
+      allOrders.sort((a, b) => new Date(b.createdAt || 0) - new Date(a.createdAt || 0));
 
-      if (orderSnap.empty) {
-        list.innerHTML = "<li class='list-group-item text-muted'>No orders yet.</li>";
-        return;
-      }
+      renderFilters();
+      renderList();
+    }
 
-      orderSnap.forEach(docSnap => {
-        const order = docSnap.data();
-        const orderId = docSnap.id;
+    function renderFilters() {
+      const counts = { all: allOrders.length };
+      STATUSES.forEach((s) => (counts[s.id] = 0));
+      allOrders.forEach((o) => {
+        const id = normStatus(o.orderStatus);
+        counts[id] = (counts[id] || 0) + 1;
+      });
 
-        const item = document.createElement("li");
-        item.className = "list-group-item mb-2";
+      const pill = (id, label) => `
+        <span class="badge rounded-pill text-bg-${id === "all" ? "dark" : statusMeta(id).badge}
+          filter-pill ${activeFilter === id ? "active" : ""}"
+          onclick="setFilter('${id}')">${label} (${counts[id] || 0})</span>`;
 
-        const itemList = order.items.map(i => {
-          const product = products[i.productId];
-          const name = product ? product.name : i.productId;
-          return `${i.qty} × ${name}`;
-        }).join(", ");
+      document.getElementById("filter-pills").innerHTML =
+        pill("all", "All") + STATUSES.map((s) => pill(s.id, s.label)).join("");
+    }
 
-        item.innerHTML = `
-          <div class="mb-2">
-            <strong>${order.customer.name}</strong> | ${order.customer.phone}<br>
-            <small>${order.customer.address}</small>
-          </div>
-          <div class="mb-2">
-            ${itemList}<br>
-            <strong>Total:</strong> ₹${order.total.toLocaleString()}
+    window.setFilter = function (id) {
+      activeFilter = id;
+      renderFilters();
+      renderList();
+    };
+
+    function historyHtml(order) {
+      const h = order.statusHistory || [];
+      if (!h.length) return "";
+      const items = [...h]
+        .reverse()
+        .map((e) => {
+          const m = statusMeta(normStatus(e.status));
+          return `<li><span class="badge text-bg-${m.badge}">${m.label}</span>
+            <small class="text-muted">${fmtDate(e.at)}</small></li>`;
+        })
+        .join("");
+      return `<details class="mt-2"><summary class="text-muted small" style="cursor:pointer">
+        Status history (${h.length})</summary>
+        <ul class="small mt-1 mb-0 ps-3">${items}</ul></details>`;
+    }
+
+    function orderCard(orderId, order, products) {
+      const st = normStatus(order.orderStatus);
+      const meta = statusMeta(st);
+      const customer = order.customer || {};
+      const itemList = (order.items || [])
+        .map((i) => {
+          const p = products[i.productId];
+          return `${i.qty} × ${esc(p ? p.name : i.productId)}`;
+        })
+        .join(", ");
+
+      const div = document.createElement("div");
+      div.className = "card shadow-sm";
+      div.innerHTML = `
+        <div class="card-body">
+          <div class="d-flex justify-content-between align-items-start mb-2">
+            <div>
+              <strong>${esc(customer.name)}</strong>
+              <span class="badge text-bg-${meta.badge} ms-2">${meta.label}</span><br>
+              <small class="text-muted">📞 ${esc(customer.phone)} · 🏠 ${esc(customer.address)}</small>
+            </div>
+            <small class="text-muted text-end">🕒 ${fmtDate(order.createdAt)}</small>
           </div>
-          <div class="d-flex align-items-center">
-            <label class="me-2">Status:</label>
-            <select class="form-select form-select-sm w-auto" onchange="updateOrderStatus('${orderId}', this.value)">
-              ${["new", "in-progress", "shipped", "delivered"].map(status => `
-                <option value="${status}" ${status === order.orderStatus ? "selected" : ""}>${status}</option>
-              `).join("")}
-            </select>
+          <div class="mb-2 small">${itemList}</div>
+          <div class="d-flex flex-wrap justify-content-between align-items-center gap-2">
+            <strong>Total: ₹${Number(order.total || 0).toLocaleString("en-IN")}</strong>
+            <div class="d-flex align-items-center gap-2">
+              <label class="small text-muted mb-0">Status:</label>
+              <select class="form-select form-select-sm w-auto"
+                onchange="updateOrderStatus('${orderId}', this.value)">
+                ${STATUSES.map(
+                  (s) => `<option value="${s.id}" ${s.id === st ? "selected" : ""}>${s.label}</option>`
+                ).join("")}
+              </select>
+            </div>
           </div>
-        `;
+          ${historyHtml(order)}
+        </div>`;
+      return div;
+    }
 
-        list.appendChild(item);
+    function renderList() {
+      const list = document.getElementById("order-list");
+      list.innerHTML = "";
+
+      const filtered = allOrders.filter((o) => {
+        if (activeFilter !== "all" && normStatus(o.orderStatus) !== activeFilter) return false;
+        if (searchTerm) {
+          const c = o.customer || {};
+          const hay = `${c.name} ${c.phone}`.toLowerCase();
+          if (!hay.includes(searchTerm)) return false;
+        }
+        return true;
       });
+
+      if (!filtered.length) {
+        list.innerHTML = "<div class='alert alert-light'>No orders found.</div>";
+        return;
+      }
+
+      filtered.forEach((o) => list.appendChild(orderCard(o.id, o, productMap)));
     }
 
     window.updateOrderStatus = async function (orderId, newStatus) {
-      const ref = doc(db, "orders", orderId);
-      await updateDoc(ref, { orderStatus: newStatus });
-      alert("✅ Order status updated.");
-    }
+      try {
+        await updateDoc(doc(db, "orders", orderId), {
+          orderStatus: newStatus,
+          updatedAt: new Date().toISOString(),
+          statusHistory: arrayUnion({ status: newStatus, at: new Date().toISOString() }),
+        });
+        showToast(`✅ Order marked: ${statusMeta(newStatus).label}`);
+        loadOrders();
+      } catch (e) {
+        console.error(e);
+        showToast("❌ Could not update status", "danger");
+      }
+    };
 
-    // Handle Edit Product Modal
+    document.getElementById("search").addEventListener("input", (e) => {
+      searchTerm = e.target.value.trim().toLowerCase();
+      renderList();
+    });
+
+    document.getElementById("logout-btn").addEventListener("click", async () => {
+      await signOut(auth);
+      window.location.href = "login.html";
+    });
+
+    // ── Edit Product Modal (existing) ────────────────────────────
     const editForm = document.getElementById("edit-product-form");
     editForm.addEventListener("submit", async (e) => {
       e.preventDefault();
@@ -146,7 +310,7 @@
       if (imageUrl) updateData.image = imageUrl;
 
       await updateDoc(doc(db, "products", id), updateData);
-      alert("✅ Product updated.");
+      showToast("✅ Product updated.");
       location.reload();
     });
 
@@ -158,7 +322,7 @@
       document.getElementById("edit-preview").src = product.image;
       document.getElementById("edit-preview").classList.remove("d-none");
       new bootstrap.Modal(document.getElementById("editProductModal")).show();
-    }
+    };
 
     loadOrders();
   </script>
diff --git a/admin/admin.html b/admin/admin.html
index 56f3b77..84f8bd1 100644
--- a/admin/admin.html
+++ b/admin/admin.html
@@ -1,7 +1,9 @@
-<script>
-  if (localStorage.getItem("vaishnavi_logged_in") !== "true") {
-    window.location.href = "login.html";
-  }
+<script type="module">
+  import { auth } from "../assets/js/firebase.js";
+  import { onAuthStateChanged } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js";
+  onAuthStateChanged(auth, (user) => {
+    if (!user) window.location.href = "login.html";
+  });
 </script>
 
 
diff --git a/assets/js/cart.js b/assets/js/cart.js
index 2ba89f1..a14b938 100644
--- a/assets/js/cart.js
+++ b/assets/js/cart.js
@@ -1,6 +1,26 @@
 // assets/js/cart.js
 const CART_KEY = "vaishnavi_cart";
 
+function showToast(msg) {
+  let container = document.getElementById("toast-container");
+  if (!container) {
+    container = document.createElement("div");
+    container.id = "toast-container";
+    container.className = "toast-container position-fixed bottom-0 end-0 p-3";
+    container.style.zIndex = "1080";
+    document.body.appendChild(container);
+  }
+  const el = document.createElement("div");
+  el.className = "toast align-items-center text-bg-success border-0";
+  el.setAttribute("role", "alert");
+  el.innerHTML = '<div class="d-flex"><div class="toast-body"></div>' +
+    '<button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button></div>';
+  el.querySelector(".toast-body").textContent = msg;
+  container.appendChild(el);
+  new bootstrap.Toast(el, { delay: 2000 }).show();
+}
+
+
 function getCart() {
   return JSON.parse(localStorage.getItem(CART_KEY)) || [];
 }
@@ -20,7 +40,7 @@ function addToCart(productId) {
   }
 
   saveCart(cart);
-  alert("Added to cart!");
+  showToast("Added to cart!");
 }
 
 function removeFromCart(productId) {
diff --git a/assets/js/products.js b/assets/js/products.js
index 36ee408..ba4afb8 100644
--- a/assets/js/products.js
+++ b/assets/js/products.js
@@ -26,7 +26,7 @@ document.addEventListener("DOMContentLoaded", async () => {
 
     col.innerHTML = `
       <div class="card shadow-sm h-100">
-        <img src="${product.image}" class="card-img-top" alt="${product.name}">
+        <img src="${product.image}" class="card-img-top" alt="${product.name}" loading="lazy">
         <div class="card-body">
           <h5 class="card-title">${product.name}</h5>
           <p class="card-text text-muted">₹${product.price.toLocaleString()}</p>
@@ -40,17 +40,3 @@ document.addEventListener("DOMContentLoaded", async () => {
     galleryGrid.appendChild(col);
   });
 });
-
-function addToCart(productId) {
-  let cart = JSON.parse(localStorage.getItem("vaishnavi_cart")) || [];
-  const index = cart.findIndex(item => item.productId === productId);
-
-  if (index !== -1) {
-    cart[index].qty += 1;
-  } else {
-    cart.push({ productId, qty: 1 });
-  }
-
-  localStorage.setItem("vaishnavi_cart", JSON.stringify(cart));
-  alert("🛒 Product added to cart!");
-}
diff --git a/checkout.html b/checkout.html
index f039c72..19ac722 100644
--- a/checkout.html
+++ b/checkout.html
@@ -111,13 +111,16 @@
 
     const total = await renderCheckoutCart();
 
+    const now = new Date().toISOString();
     const order = {
       customer: { name, phone, address },
       items: cart,
       total,
-      orderStatus: "new",
+      orderStatus: "received",
       paymentStatus: "cod",
-      createdAt: new Date().toISOString()
+      statusHistory: [{ status: "received", at: now }],
+      createdAt: now,
+      updatedAt: now
     };
 
     try {
diff --git a/data/products.json b/data/products.json
deleted file mode 100644
index 156f7b5..0000000
--- a/data/products.json
+++ /dev/null
@@ -1,62 +0,0 @@
-[
-  {
-    "id": "mandir001",
-    "name": "Carved Teak Mandir",
-    "category": "mandir",
-    "price": 12500,
-    "stock": 3,
-    "dimensions": "36x18x48 in",
-    "image": "assets/images/mandir001.jpg",
-    "description": "Beautifully handcrafted teakwood mandir with traditional arch design."
-  },
-  {
-    "id": "cnc001",
-    "name": "Modern CNC Panel",
-    "category": "cnc",
-    "price": 6800,
-    "stock": 5,
-    "dimensions": "48x24 in",
-    "image": "assets/images/cnc001.jpg",
-    "description": "Contemporary CNC-cut floral panel perfect for living room dividers."
-  },
-  {
-    "id": "jali001",
-    "name": "Laser Jali Partition",
-    "category": "jali",
-    "price": 4200,
-    "stock": 7,
-    "dimensions": "36x72 in",
-    "image": "assets/images/jali001.jpg",
-    "description": "Elegant MDF jali with geometric design, great for space separation."
-  },
-  {
-    "id": "design3d001",
-    "name": "3D Lotus Panel",
-    "category": "design3d",
-    "price": 9800,
-    "stock": 4,
-    "dimensions": "24x36 in",
-    "image": "assets/images/design3d001.jpg",
-    "description": "3D carved lotus wall panel that adds depth and elegance to decor."
-  },
-  {
-    "id": "kitchen001",
-    "name": "Modular Kitchen Set",
-    "category": "kitchen",
-    "price": 45500,
-    "stock": 2,
-    "dimensions": "10x8 ft",
-    "image": "assets/images/kitchen001.jpg",
-    "description": "Stylish modular kitchen with soft-close drawers and CNC cabinet fronts."
-  },
-  {
-    "id": "showcase001",
-    "name": "Wooden Showcase Unit",
-    "category": "showcase",
-    "price": 11800,
-    "stock": 6,
-    "dimensions": "60x18x72 in",
-    "image": "assets/images/showcase001.jpg",
-    "description": "Multi-shelf showcase unit with glass panels and storage drawers."
-  }
-]
\ No newline at end of file
diff --git a/firebase.json b/firebase.json
index c9bd494..41faf52 100644
--- a/firebase.json
+++ b/firebase.json
@@ -5,6 +5,12 @@
       "firebase.json",
       "**/.*",
       "**/node_modules/**"
+    ],
+    "headers": [
+      {
+        "source": "/assets/**",
+        "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
+      }
     ]
   }
 }

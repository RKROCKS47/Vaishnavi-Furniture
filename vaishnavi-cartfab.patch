diff --git a/assets/js/cart.js b/assets/js/cart.js
index a14b938..8fe8bd4 100644
--- a/assets/js/cart.js
+++ b/assets/js/cart.js
@@ -29,6 +29,27 @@ function saveCart(cart) {
   localStorage.setItem(CART_KEY, JSON.stringify(cart));
 }
 
+function cartCount() {
+  return getCart().reduce((n, item) => n + (item.qty || 0), 0);
+}
+
+// Floating "View Cart" button: show when cart has items, update badge, pulse on add
+function updateCartFab(pulse) {
+  const fab = document.getElementById("cart-fab");
+  if (!fab) return;
+  const n = cartCount();
+  const badge = document.getElementById("cart-fab-count");
+  if (badge) badge.textContent = n;
+  fab.classList.toggle("d-none", n === 0);
+  if (pulse && n > 0) {
+    fab.classList.remove("pulse");
+    void fab.offsetWidth; // restart the animation
+    fab.classList.add("pulse");
+  }
+}
+
+document.addEventListener("DOMContentLoaded", () => updateCartFab(false));
+
 function addToCart(productId) {
   let cart = getCart();
   const item = cart.find(i => i.productId === productId);
@@ -41,6 +62,7 @@ function addToCart(productId) {
 
   saveCart(cart);
   showToast("Added to cart!");
+  updateCartFab(true);
 }
 
 function removeFromCart(productId) {
diff --git a/index.html b/index.html
index 88904f8..5a64e23 100644
--- a/index.html
+++ b/index.html
@@ -61,6 +61,29 @@
     .navbar-nav .nav-link:hover { color: #8B4513; }
     .tagline { font-size: 0.8rem; color: gray; margin-top: -10px; }
     .filter-btn.active { background-color: #A0522D; color: white; }
+    /* Floating "View Cart" button */
+    #cart-fab {
+      position: fixed; bottom: 24px; right: 24px; z-index: 1050;
+      width: 62px; height: 62px; border-radius: 50%;
+      background: #198754; color: #fff; font-size: 27px;
+      display: flex; align-items: center; justify-content: center;
+      box-shadow: 0 4px 14px rgba(0,0,0,.3); text-decoration: none;
+      transition: transform .2s;
+    }
+    #cart-fab:hover { transform: scale(1.08); color: #fff; }
+    #cart-fab-count {
+      position: absolute; top: -6px; right: -6px;
+      background: #dc3545; color: #fff; border-radius: 999px;
+      min-width: 24px; height: 24px; font-size: 13px; font-weight: bold;
+      display: flex; align-items: center; justify-content: center;
+      padding: 0 6px; border: 2px solid #fff;
+    }
+    #cart-fab.pulse { animation: fabPulse .5s ease; }
+    @keyframes fabPulse {
+      0% { transform: scale(1); }
+      40% { transform: scale(1.25); }
+      100% { transform: scale(1); }
+    }
   </style>
 </head>
 
@@ -396,6 +419,11 @@ ${message}
 
   <!-- Bootstrap JS -->
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
+  <!-- Floating "View Cart" button (appears when cart has items) -->
+  <a href="cart.html" id="cart-fab" class="d-none" aria-label="Go to cart">
+    🛒<span id="cart-fab-count">0</span>
+  </a>
+
   <script defer src="assets/js/cart.js"></script>
   <script type="module" src="assets/js/products.js"></script>
 </body>
